ROUND: 8
AUTHOR: codex
READS: draft.md, r7.claude.md
VERDICT: needs-work
OPEN_BLOCKERS: 0
OPEN_MAJORS: 2

[SEVERITY: major]
LENS: completeness
LOCATION: 3.1 “Puffery” / 8.2 “antislop-check”
ISSUE: Tier P permits puffery copied verbatim from a user-approved brand claim, but standalone checking has no provenance input to distinguish that exception from model-invented puffery.
SUGGESTION: Define an approved-claims/source-span input contract for checking, and require unknown provenance to be flagged or explicitly confirmed.

[SEVERITY: major]
LENS: testability
LOCATION: 10 “Runner”
ISSUE: The headless runner commands do not specify how a fresh checkout makes the local plugin and `antislop-check` skill available to Claude/Codex, so CI can invoke an agent that never loads the skill.
SUGGESTION: Specify a reproducible per-backend test setup that installs or mounts the checked-out plugin, selects it explicitly, and cleans up afterward.