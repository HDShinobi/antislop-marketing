ROUND: 28
AUTHOR: codex
READS: draft.md, r27.claude.md
VERDICT: needs-work
OPEN_BLOCKERS: 0
OPEN_MAJORS: 2

[SEVERITY: major]
LENS: testability
LOCATION: §10 “Tầng 3, repo tự soi mình”
ISSUE: CI must scan README and examples, but `scan()` requires an explicit tier and `lang`/`langMap`, while no deterministic tier/language configuration is defined for those files.
SUGGESTION: Define a checked-in per-file scan manifest (tier, lang/langMap, optional tierMap) for README and every example, and require CI to invoke scan from it.

[SEVERITY: major]
LENS: design
LOCATION: §3.2 “Chuẩn R cho vùng số liệu” and §10 “Tier cũng không đồng nhất trong một tài liệu”
ISSUE: `scan.mjs` applies table/tierMap R overrides mechanically, but its output schema exposes neither effective tier nor detected R blocks, forcing `antislop-check` to re-detect them to apply R-only semantic evidence rules.
SUGGESTION: Emit effective tier per block/finding (or a complete block-tier map) from `scan.mjs`, and require `antislop-check` to use that output for judged findings.