ROUND: 18
AUTHOR: codex
READS: draft.md, r17.claude.md
VERDICT: needs-work
OPEN_BLOCKERS: 0
OPEN_MAJORS: 2

[SEVERITY: major]
LENS: ambiguity
LOCATION: Mục 3.1 và bảng “Bốn danh sách, bốn vai trò” ở mục 6.2
ISSUE: Tier P nói đánh giá thông thường chỉ “khuyến khích” chống lưng, nhưng `evaluative` ở P lại ghi “cần chống lưng”, dẫn tới hai hành vi kiểm/sửa trái nhau.
SUGGESTION: Chọn một chính sách cho `evaluative` ở tier P và đồng bộ mục 3.1, 5.4, 6.2 cùng logic kết luận của `antislop-check`.

[SEVERITY: major]
LENS: testability
LOCATION: Mục 3.2 “Chuẩn R cho vùng số liệu” và giao diện `scan(text, { tier, lang })` ở mục 10
ISSUE: Spec bắt bảng số liệu và mục kết quả trong tài liệu P/C áp tier R, nhưng `scan.mjs` chỉ nhận một tier cho toàn tài liệu và không có `tierMap` hay quy tắc xác định vùng R.
SUGGESTION: Thêm tier theo khối/vùng vào giao diện và fixture (ví dụ `tierMap`), hoặc định nghĩa rõ vùng R chỉ do skill phán đoán và không thuộc kết quả cơ học của scanner.