ROUND: 17
AUTHOR: claude
READS: draft.md, r17.codex.md
VERDICT: needs-work
OPEN_BLOCKERS: 0
OPEN_MAJORS: 0

ACCEPT [major/testability] API theo khối không dựng được span tuyệt đối
  Đúng, và hai phần của spec đánh nhau. Vòng 7 tôi bảo người gọi tách khối rồi
  gọi scan hai lần; vòng 16 tôi bắt findings mang span tính từ đầu file và chỉ
  số khối. Hàm nhận một khối rời thì không biết cả hai thứ đó.

ACCEPT [major/testability] Hai dòng trong bảng ĐẾM ĐƯỢC không có nguồn
  Đúng. Bảng 8.1 hiển thị "đoạn 1-2 câu" và "dấu hai chấm ngoài danh sách" như
  số tái lập, nhưng schema đầu ra của scan.mjs không có hai khoá đó và không
  định nghĩa cách tính.

## Sửa trong draft.md

- Mục 10, giao diện: scan LUÔN nhận trọn tài liệu, tự tách khối, nên nó là chỗ
  duy nhất biết offset tuyệt đối và chỉ số khối. Song ngữ khai bằng `langMap`
  theo chỉ số khối thay vì gọi nhiều lần. Thêm dạng CLI `--lang-map`.
- Bảng gộp giữa các khối giữ nguyên nhưng nói rõ scan làm việc đó bên trong;
  quy tắc vẫn phải chốt vì fixture assert kết quả cuối. Thêm dòng cho
  `short_paragraph_ratio` là tính lại trên toàn tài liệu chứ không gộp từ khối.
- Schema đầu ra: `counted` chốt đúng tám khoá, thêm `colon_outside_list` và
  `short_paragraph_ratio`. Định nghĩa thuật toán cho cả hai. `short_paragraph_ratio`
  xuất dạng cặp số nguyên chứ không phải số thực, vì cặp số nguyên so khớp chính
  xác được trong fixture còn số thực thì không.
- Cả hai đếm mới thuần cơ học, không cần pack, nên vẫn chạy với ngôn ngữ chưa
  đăng ký. Bổ sung vào bảng đó.
- Bảng 8.1 đồng bộ đúng tám khoá, không hơn không kém.

## Dọn thêm, không do critic nêu

- Schema JSON của antislop-check còn kê bốn khoá counted, sửa thành tám.
- Ba chỗ còn dùng mã rule cũ `EVID-NO-NUMBER` và `VI-BAN-PUFFERY`, đổi sang
  `VI-EVAL-CANDIDATE` và `VI-PUFFERY` cho khớp bảng bốn danh sách ở mục 6.2.
- Mục 2 còn nhắc mã `EVID-CANDIDATE`, đổi sang `*-PUFFERY` và `*-COMPARATIVE`.
- Mục 11 nói "ba dòng trong bảng đếm", đã lỗi thời sau khi bảng lên tám dòng;
  viết lại thành giải thích ba loại giá trị của cột ngưỡng.
