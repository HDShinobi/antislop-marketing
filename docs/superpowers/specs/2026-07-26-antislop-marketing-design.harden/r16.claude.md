ROUND: 16
AUTHOR: claude
READS: draft.md, r16.codex.md
VERDICT: needs-work
OPEN_BLOCKERS: 0
OPEN_MAJORS: 0

ACCEPT [major/design] Lớp `khac` biến same_shape_run thành máy sinh dương tính giả
  Đúng, và tệ hơn là chính tôi đã nêu lo ngại này ở vòng 13 rồi gạt đi bằng câu
  "chấp nhận được vì chữ ký còn hai thành phần nữa". Sai. Ba câu trần thuật bình
  thường, cùng hai vế, không đuôi bổ nghĩa, đều ra (khac, 2, false) và bị báo dù
  chúng chẳng giống nhau chút nào.

ACCEPT [major/testability] Bảng 8.1 hứa số tất định cho một phán xử ngữ nghĩa
  Đúng. Dòng "đánh giá không được chống lưng, ngưỡng 0" nằm trong khối ĐẾM ĐƯỢC,
  trong khi mục 10 đã chốt chống lưng là câu hỏi ngữ nghĩa không assert được.
  Hai mục nói ngược nhau.

ACCEPT [major/completeness] findings của scan.mjs không có schema
  Đúng. Vòng 15 tôi bắt model chép nguyên `findings_mechanical` từ scan.mjs
  nhưng chưa bao giờ nói scan.mjs xuất finding ở dạng gì. API mới chỉ có
  `counted` và một tập `rules`.

## Sửa trong draft.md

- Mục 10, định nghĩa cùng khuôn: viết lại thành ba điều kiện, trong đó điều kiện
  1 thêm vế "và lớp đó không phải `khac`". Hai câu cùng rơi vào `khac` không
  tính là cùng khuôn. Giải thích vì sao đây là điều kiện quan trọng nhất, và vì
  sao giới hạn vào lớp đóng lại trung thành với quan sát gốc của adenaufal
  (The/This/It/In đều là từ chức năng thuộc lớp đóng).
- Mục 8.1: đổi dòng bảng thành `eval_candidate (ứng viên)` không có ngưỡng, thêm
  hai dòng vào khối PHÁN ĐOÁN cho bằng chứng chống lưng và mốc so sánh, ghi tỉ
  lệ thay vì ngưỡng. Thêm đoạn giải thích vì sao đặt ngưỡng 0 cho một con số ứng
  viên là hứa độ chính xác mà phép quét không có.
- Mục 10, thêm "Đầu ra của scan.mjs": schema JSON đầy đủ gồm `counted` và
  `findings`, cùng bảng năm quy ước bắt buộc (span theo ký tự Unicode nửa mở,
  text trích đúng, lang, block, thứ tự sắp theo span). Nhấn mạnh offset phải
  theo ký tự chứ không theo byte, vì chữ tiếng Việt có dấu chiếm nhiều byte
  trong UTF-8 và trộn hai cách đếm thì span trỏ sai chỗ.
- `.expect.json` của fixture cơ học dùng chung schema này.
