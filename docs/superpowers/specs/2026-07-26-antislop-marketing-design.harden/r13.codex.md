ROUND: 13
AUTHOR: codex
READS: draft.md, r12.claude.md
VERDICT: needs-work
OPEN_BLOCKERS: 0
OPEN_MAJORS: 3

[SEVERITY: major]
LENS: testability
LOCATION: 6.2 “Khối máy đọc được” vs. 6.5 “Nội dung vi.md”
ISSUE: Spec says token lists exist only in `antislop-pack`, but 6.5 adds different ban-list phrases and rules absent from that schema, so the skill and `scan.mjs` will enforce different rule sets.
SUGGESTION: Move every machine-enforced token into the YAML schema and make 6.5 explanatory examples only, or explicitly separate non-scannable advisory rules with their own IDs and expected behavior.

[SEVERITY: major]
LENS: testability
LOCATION: 10 “eval_candidate”
ISSUE: “Token dữ kiện” includes units, dates, URLs, and configuration-field names, but no grammar, canonical unit list, or configuration-field source is defined, so implementations cannot deterministically produce the asserted count.
SUGGESTION: Define exact detection grammars and a schema field for configuration-field tokens (or limit the mechanical detector to precisely specified patterns).

[SEVERITY: major]
LENS: ambiguity
LOCATION: 10 “lớp_từ_mở_đầu”
ISSUE: The six opener lists cannot cover arbitrary nouns, proper names, adjectives, or unknown first tokens, and the spec gives no fallback class, making `same_shape_run` undefined for ordinary sentences.
SUGGESTION: Add a required fallback opener class and deterministic precedence rules when a token matches multiple classes.