ROUND: 4
AUTHOR: claude
READS: draft.md, r4.codex.md
VERDICT: needs-work
OPEN_BLOCKERS: 0
OPEN_MAJORS: 0

ACCEPT [major/design] Ví dụ tier C tự vi phạm quy tắc vừa đặt ở 5.4
  Đúng, và chính ví dụ của tôi là vật chứng. "Lọc tới 0.0001 micron, vượt xa
  chuẩn thường thấy" không nêu chuẩn thường thấy là bao nhiêu, nên con số không
  chứng minh cụm "vượt xa". Đây đúng là kiểu lách mà mục 5.4 vừa cấm, chỉ khác
  là nó nằm trong ví dụ ĐẠT của chính tôi ba mục phía trên.

ACCEPT [major/testability] scan.mjs không thể quyết định quan hệ chứng minh
  Đúng, và đây là mâu thuẫn nặng nhất còn lại. Tôi đòi một máy quét thuần từ
  vựng cho ra con số tất định về một câu hỏi ngữ nghĩa. Regex không trả lời được
  "dữ kiện này có chứng minh tính từ kia không". Nếu để nguyên thì hoặc scan.mjs
  không viết được, hoặc nó được viết ẩu rồi CI khẳng định một con số vô nghĩa.

## Sửa trong draft.md

- Mục 3.1: tier C nói rõ chỉ nới ở chỗ cho phép tính từ mạnh, không nới quan hệ
  chứng minh. Thêm yêu cầu riêng cho claim so sánh và cực cấp: mốc so sánh phải
  nêu ra, không để ngầm. Thay ví dụ hỏng bằng một cặp KHÔNG ĐẠT và hai cách ĐẠT.
- Mục 10 tầng 1: thêm mục "Ranh giới năng lực của scan.mjs", tách một khái niệm
  thành hai. `eval_candidate` thuần từ vựng và vị trí, do scan.mjs quyết, tất
  định, được assert. `evidence_backing` là câu hỏi ngữ nghĩa, do antislop-check
  quyết, nằm ở khối judged, không bao giờ assert. Nói thẳng rằng scan.mjs báo
  thừa ở chỗ này và đó là thiết kế, giống linter cảnh báo chứ không tuyên án.
- Đổi tên toàn bộ `eval_unbacked` thành `eval_candidate` và `EVID-UNBACKED`
  thành `EVID-CANDIDATE` trong schema JSON, fixture cơ học, fixture tầng 2 và
  bảng mục 8.1, để tên phản ánh đúng thứ nó đo.
- Mục 3.1 và 3.2: dọn hai chỗ còn sót nói "kèm số" và "kèm dữ kiện" cho khớp
  thuật ngữ "chống lưng".

## Ghi chú về trạng thái vòng lặp

Đây là vòng MAX_ROUNDS. Hai major của vòng này đã sửa, nhưng bản sửa CHƯA qua
một vòng critic nào. Theo định nghĩa hội tụ ở PROTOCOL, đây không phải hội tụ:
chưa có vòng Codex nào báo OPEN_BLOCKERS 0 và OPEN_MAJORS 0. Cần người dùng
quyết định chạy thêm một vòng xác minh hay dừng.
