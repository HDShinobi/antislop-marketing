ROUND: 30
AUTHOR: claude
READS: draft.md, r30.codex.md
VERDICT: needs-work
OPEN_BLOCKERS: 0
OPEN_MAJORS: 0

ACCEPT [major/ambiguity] tierMap hạ được bảng số liệu xuống dưới R
  Đúng, mâu thuẫn trực tiếp. Mục 3.2 nói chuẩn R cho vùng số liệu không ghi đè
  được; vòng 18 tôi lại chốt "tierMap thắng bảng-tự-nhận". Hai câu không thể
  cùng đúng.

ACCEPT [major/testability] Fixture khai mã cơ học nhưng runner tìm ở nhóm phán xử
  Đúng. Vòng 26 tôi chuyển must_flag sang chỉ áp cho findings_judged mà không
  sửa ví dụ fixture, vốn đang khai VI-EVAL-CANDIDATE và VI-PUFFERY là mã cơ học.
  Fixture đó không bao giờ đạt.

ACCEPT [minor/completeness] Không có schema chính thức cho antislop-pack
  Đúng. Spec nhắc "pack sai schema" hai lần mà chưa bao giờ định nghĩa schema
  nào, nên không nói được thế nào là sai và không viết được kiểm CI cho pack mới.

## Sửa trong draft.md

- Chốt chuẩn R cho vùng số liệu là SÀN. Thứ tự nghiêm C < P < R, và tier hiệu
  lực là max của ba nguồn. tierMap chỉ nâng được, không hạ được. Khai tier C cho
  một bảng số liệu thì khối đó vẫn R và tier_source vẫn là data_table.
- Lý do: quy tắc vùng số liệu tồn tại để chặn đúng một lỗi, là proposal chứa
  bảng kết quả với tính từ không chống lưng. Cho ghi đè thì nó thành lời khuyên.
- Fixture tầng 2: must_flag và must_not_flag chỉ khai mã phán xử. Sửa ví dụ sang
  EVID-UNBACKED và VI-PUFFERY-UNLISTED. Giải thích phần cơ học đã được
  deep-compare nên khai lại vừa thừa vừa sai chỗ.
- Thêm bảng schema đầy đủ của antislop-pack: 13 khoá, kiểu, bắt buộc, rỗng được
  hay không. Mọi khoá bắt buộc có mặt; rỗng là một tuyên bố còn thiếu là một
  thiếu sót.
- Khoá lạ thì scan.mjs bỏ qua nhưng CI cảnh báo, để thử nghiệm được mà không im
  lặng tích tụ.
- Thêm `bin/validate-pack.mjs` vào cây thư mục và vào CI, kèm bốn fixture bắt
  buộc: pack hợp lệ, thiếu khoá, sai kiểu, có khoá lạ.
