ROUND: 29
AUTHOR: codex
READS: draft.md, r28.claude.md
VERDICT: needs-work
OPEN_BLOCKERS: 0
OPEN_MAJORS: 2

[SEVERITY: major]
LENS: ambiguity
LOCATION: Mục 3.0 và mục 10, “bảng số liệu” / `tierMap`
ISSUE: Spec định nghĩa mỗi ô bảng là một khối, nhưng tự nhận bảng và ví dụ `blocks` lại xử bảng như một khối; không rõ tier R được gán cho ô nào và `tierMap` trỏ tới ô hay toàn bảng.
SUGGESTION: Quy định bảng là một parent container với các block ô con, rồi nêu rõ bảng đạt tiêu chí số liệu sẽ gán tier R cho mọi ô con và `tierMap` nhận index của block nào.

[SEVERITY: major]
LENS: design
LOCATION: Mục 4.2 và mục 10, `langMap`
ISSUE: API song ngữ yêu cầu `langMap` theo chỉ số block, trong khi chính `scan` là nơi duy nhất tách và công bố chỉ số block, buộc caller phải tự tái lập phép tách trước khi gọi scan.
SUGGESTION: Đổi `langMap` sang ánh xạ theo span/heading hoặc thêm pha API tách khối công khai để caller lấy index trước; xác định rõ một đường duy nhất cho antislop-check tạo map.