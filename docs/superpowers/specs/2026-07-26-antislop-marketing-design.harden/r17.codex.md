ROUND: 17
AUTHOR: codex
READS: draft.md, r16.claude.md
VERDICT: needs-work
OPEN_BLOCKERS: 0
OPEN_MAJORS: 2

[SEVERITY: major]
LENS: testability
LOCATION: Mục 10, “Giao diện của scan.mjs” và “Đầu ra của scan.mjs”
ISSUE: Luồng song ngữ bảo tách khối rồi gọi `scan(text, { tier, lang })`, nhưng API không nhận offset hay chỉ số khối gốc nên không thể tạo `span` theo toàn file và `block` đúng như schema bắt buộc.
SUGGESTION: Cho `scan` nhận toàn tài liệu cùng map ngôn ngữ theo khối, hoặc bổ sung bắt buộc `baseOffset` và `blockIndex` khi quét từng khối rồi quy định cách caller gộp kết quả.

[SEVERITY: major]
LENS: testability
LOCATION: Mục 8.1 và Mục 10, “Đầu ra của scan.mjs”
ISSUE: Bảng “ĐẾM ĐƯỢC” tuyên bố các số tái lập và `counted` phải chép từ `scan.mjs`, nhưng `scan.mjs`/schema không định nghĩa hay xuất số đoạn 1–2 câu và dấu hai chấm, còn schema mẫu cũng không có hai trường này.
SUGGESTION: Hoặc thêm thuật toán, khóa `counted`, findings và fixture cho hai phép đếm đó vào `scan.mjs`, hoặc chuyển chúng khỏi phần đếm tái lập sang phán đoán/loại khỏi báo cáo chuẩn.