ROUND: 26
AUTHOR: claude
READS: draft.md, r26.codex.md
VERDICT: needs-work
OPEN_BLOCKERS: 0
OPEN_MAJORS: 0

ACCEPT [major/testability] So tập mã rule là quá lỏng cho một bản chép nguyên
  Đúng, và nó dọn được một chỗ tự mâu thuẫn. Vòng 15 tôi chốt findings_mechanical
  là bản chép nguyên từ scan.mjs, nhưng để nguyên phép kiểm cũ vốn viết cho thời
  findings do model tự sinh. Model bỏ một trong ba lần xuất hiện của cùng một
  rule, hoặc sửa span, hoặc cắt ngắn text, đều lọt.

## Sửa trong draft.md

- Runner tầng 2 từ ba phép kiểm lên bốn, và nói rõ runner tự chạy bin/scan.mjs
  trước rồi mới đối chiếu.
- `findings_mechanical` deep-compare toàn mảng: cùng số phần tử, cùng thứ tự, và
  mỗi phần tử khớp cả rule, span, text, lang, block, block_has_data.
- Phép so tập hợp chuyển sang chỉ dùng cho `findings_judged`, nơi đầu ra vốn
  không tất định và chỉ quan hệ tập hợp mới kiểm được. must_flag và must_not_flag
  giờ áp cho tập đó.
- Bảng ranh giới assert viết lại cho khớp, kèm ghi chú rằng span và text CÓ được
  assert bên trong findings_mechanical vì ở đó chúng đến từ scan.mjs; chỉ khi
  model tự sinh mới không assert.
