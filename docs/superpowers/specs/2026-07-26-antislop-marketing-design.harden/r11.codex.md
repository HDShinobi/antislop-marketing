ROUND: 11
AUTHOR: codex
READS: draft.md, r10.claude.md
VERDICT: needs-work
OPEN_BLOCKERS: 0
OPEN_MAJORS: 1

[SEVERITY: major]
LENS: testability
LOCATION: Mục 6.2 và mục 10, `scan.mjs`
ISSUE: Language pack chỉ quy định tám mục Markdown cho người đọc, nhưng `scan.mjs` cần trích xuất ban list, sáu lớp từ mở đầu và tack-on một cách tất định; không có schema/cú pháp chuẩn nên mỗi implementer phải tự đặt cách mã hoá hoặc hard-code lại rule.
SUGGESTION: Quy định các phần máy đọc được trong mỗi pack bằng một schema chuẩn (ví dụ JSON/YAML fenced block có khoá, thứ tự ưu tiên và quy tắc so khớp), rồi bắt `scan.mjs` chỉ đọc schema đó.