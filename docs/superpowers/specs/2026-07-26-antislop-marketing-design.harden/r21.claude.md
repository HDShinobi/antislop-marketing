ROUND: 21
AUTHOR: claude
READS: draft.md, r21.codex.md
VERDICT: needs-work
OPEN_BLOCKERS: 0
OPEN_MAJORS: 0

ACCEPT [major/testability] Hai schema finding không khớp nhau
  Đúng. scan.mjs xuất finding có `lang` và `block`; ví dụ JSON của
  antislop-check bỏ hai trường đó, trong khi model bị buộc chép nguyên. Hai
  contract không thể cùng đúng.

ACCEPT [major/completeness] YAML không có parser sẵn trong Node
  Đúng, và là lỗi thực tế nhất vòng này. Node có JSON.parse dựng sẵn nhưng không
  có YAML. Thêm dependency parser thì mâu thuẫn với chính quyết định ở mục 10 là
  giữ bin/scan.mjs không phụ thuộc thư viện ngoài để kết quả không đổi theo
  phiên bản.

ACCEPT [major/completeness] Điều kiện (a) không có đường nhận tài liệu nguồn
  Đúng. Vòng 8 tôi bịt provenance cho điều kiện (b) bằng .antislop-claims.txt
  rồi để nguyên (a). Dữ kiện chống lưng có thể nằm ở file khác, và check không
  có cách nào biết.

ACCEPT [minor/ambiguity] Hợp đồng mục 7 còn ghi sáu lớp từ mở đầu
  Đúng, sót lại từ vòng 13 khi rút xuống năm lớp.

## Sửa trong draft.md

- Khối máy đọc được chuyển từ YAML sang JSON, kèm lý do. Viết lại toàn bộ ví dụ
  schema pack dưới dạng JSON hợp lệ. JSON xấu hơn khi đọc bằng mắt, nhưng đây là
  dữ liệu cho máy còn phần cho người đã nằm ở tám mục văn xuôi.
- Ví dụ JSON của antislop-check bổ sung `lang` và `block` vào cả
  findings_mechanical lẫn findings_judged, khớp schema scan.mjs.
- Hợp đồng mục 7 sửa thành bốn lớp đóng, ghi rõ lớp thứ năm `khac` là dự phòng
  và không khai trong pack.
- Thêm mục "Điều kiện (a) cũng cần nguồn": bảng ba tình huống với verdict riêng
  cho từng cái, và cách người dùng chỉ định nguồn. Chốt `chưa xác định` là
  verdict thứ ba bên cạnh `đạt` và `vi phạm`, hiển thị riêng trong bảng. Gộp nó
  vào `vi phạm` thì mọi lần soát trong cô lập đều đỏ rực và người dùng bỏ qua
  công cụ; gộp vào `đạt` thì công cụ nói dối.
