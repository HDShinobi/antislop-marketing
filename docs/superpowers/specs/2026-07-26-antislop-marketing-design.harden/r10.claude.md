ROUND: 10
AUTHOR: claude
READS: draft.md, r10.codex.md
VERDICT: needs-work
OPEN_BLOCKERS: 0
OPEN_MAJORS: 0

ACCEPT [major/completeness] Không có thứ tự ưu tiên khi chọn ngôn ngữ
  Đúng, và đây là ca xảy ra hàng ngày trong việc thật của người dùng: gõ tiếng
  Việt để yêu cầu soát một tài liệu tiếng Anh. Câu "ngôn ngữ yêu cầu hoặc ngôn
  ngữ tài liệu nguồn" ở bước 3 cho phép cả hai cách đọc, và một trong hai cách
  sẽ viết lại tài liệu tiếng Anh thành tiếng Việt.

ACCEPT [major/testability] counted có thể do model đếm nhẩm mà test vẫn xanh
  Đúng. Tôi tuyên bố counted tái lập được nhưng không bao giờ bắt skill lấy nó
  từ scan.mjs, và vòng 5 tôi còn hạ mức đối chiếu model với scanner xuống mức
  ghi log. Nên con số trong JSON có thể là ước lượng của model mà không ai biết.

## Sửa trong draft.md

- Mục 4, bước 3 trỏ sang mục 4.2 mới.
- Mục 4.2 mới: chốt "ngôn ngữ của lời yêu cầu không quyết định gì cả". check
  dùng đúng một tín hiệu là ngôn ngữ của khối đang soát, không có dự phòng.
  write xét ba tầng, và ngôn ngữ hội thoại chỉ là dự phòng cuối khi không có
  nguồn nào, không phải mặc định.
- Mục 10: thêm trường bắt buộc counted_source với hai giá trị scan và model.
  scan là chế độ chuẩn: chạy scan.mjs và chép nguyên, không sửa số nào. model là
  chế độ suy giảm cho harness không có Bash hoặc Node, và khi đó bảng cho người
  đọc phải ghi rõ số là ước lượng.
- Mục 10: runner fail nếu counted_source khác scan, xếp cùng nhóm lỗi môi trường
  với canary. Đảo lại quyết định vòng 5 về việc chỉ ghi log chênh lệch: khi
  counted_source là scan thì hai số phải trùng khít, lệch là fail.
- Mục 10: schema đổi từ bốn khoá thành năm khoá.
