ROUND: 15
AUTHOR: codex
READS: draft.md, r14.claude.md
VERDICT: needs-work
OPEN_BLOCKERS: 0
OPEN_MAJORS: 3

[SEVERITY: major]
LENS: design
LOCATION: 6.2 “Bốn danh sách, bốn vai trò” (`comparative`, tier R)
ISSUE: Cấm mọi dấu hiệu so sánh ở tier R làm báo cáo không thể nêu so sánh có căn cứ như “CPA cao hơn mục tiêu 10%” hoặc “giảm so với tháng trước”, dù đây là cách diễn đạt minh bạch nhất.
SUGGESTION: Cho phép `comparative` ở R khi cùng khối nêu rõ đối tượng/mốc và dữ kiện so sánh; chỉ báo lỗi khi thiếu mốc hoặc bằng chứng.

[SEVERITY: major]
LENS: testability
LOCATION: 10 “Tách câu”
ISSUE: Điều kiện token sau dấu câu phải bắt đầu bằng chữ hoa, số, hoặc ngoặc khiến các câu hợp lệ bắt đầu bằng chữ thường không được tách, nên `same_shape_run` sai trên văn bản thực tế.
SUGGESTION: Định nghĩa rõ các ranh giới hợp lệ sau dấu câu không phụ thuộc chữ hoa, đồng thời thêm fixture cho câu kế tiếp mở đầu bằng chữ thường.

[SEVERITY: major]
LENS: testability
LOCATION: 10 “Ranh giới cái gì được assert” và Runner tầng 2
ISSUE: Runner assert tập `rule` do model trả về là tất định, nhưng contract không buộc `findings` phải được dẫn xuất nguyên vẹn từ kết quả `scan.mjs`; model vẫn có thể bỏ/thêm rule, làm fixture flake.
SUGGESTION: Bắt buộc skill đưa các finding cơ học từ `scan.mjs` vào một trường riêng và runner chỉ assert trường đó; giữ finding/phán đoán của model ngoài các assert tất định.