ROUND: 7
AUTHOR: claude
READS: draft.md, r7.codex.md
VERDICT: needs-work
OPEN_BLOCKERS: 0
OPEN_MAJORS: 0

ACCEPT [major/design] Cộng same_shape_run giữa hai khối ngôn ngữ là sai phép
  Đúng, lỗi số học đơn giản mà tôi không thấy. `same_shape_run` là cực đại của
  một chuỗi liên tiếp, không phải tổng số lần. Hai khối mỗi khối run 2 cộng lại
  thành 4 và báo giả vượt ngưỡng 3. Chuỗi câu cũng không nối qua ranh giới ngôn
  ngữ, nên max() mới là phép đúng về mặt ngữ nghĩa chứ không chỉ về số.

ACCEPT [major/completeness] antislop-check không nạp evidence.md
  Đúng. Bảng nạp ở mục 2 viết từ trước khi có mục 5.4, và tôi không cập nhật nó
  khi thêm `EVID-CANDIDATE`. Check phải xét `evidence_backing` và trả về mã đó
  nhưng không nạp file định nghĩa chúng.

ACCEPT [major/ambiguity] Ngôn ngữ chưa đăng ký vẫn chạy "nhịp câu" là bất khả
  Đúng, và đây là mâu thuẫn tôi tạo ra ở chính vòng 6. Mục 6.3 nói chạy phần
  trung tính gồm nhịp câu, nhưng vòng 6 vừa định nghĩa nhịp câu cần bảng phân
  lớp từ mở đầu và danh sách tack-on, cả hai chỉ có trong language pack.

## Sửa trong draft.md

- Mục 10, phần song ngữ: thêm bảng cách gộp từng bộ đếm. `dash`, `banlist`,
  `eval_candidate` cộng; `same_shape_run` lấy max. `findings` giữ `span` theo
  khối kèm trường `lang`.
- Mục 2, bảng nạp: viết lại thành năm dòng theo tier, thêm `evidence.md` vào mọi
  dòng kể cả tier C, kèm lý do (tier C vẫn áp kiểm bằng chứng cho puffery và
  claim so sánh). Nói rõ không gộp quy tắc bằng chứng vào `core.md` để tiết kiệm
  một lần nạp, vì làm thế phá nguyên tắc một-rule-một-chỗ.
- Mục 10, thêm "Ngôn ngữ chưa đăng ký": bảng bốn dòng chốt cái gì chạy, cái gì
  trả `null`. Chọn phương án bỏ `same_shape_run` thay vì dựng bảng phân lớp
  trung tính, vì sáu lớp từ mở đầu cần token của từng ngôn ngữ và một bảng chung
  sẽ hoặc rỗng hoặc sai cho mọi ngôn ngữ.

  Quy định dùng `null` chứ không dùng `0`: số 0 nghĩa là đã kiểm và sạch, `null`
  nghĩa là không kiểm được. Nhầm hai cái này thì người dùng tưởng văn bản đã qua
  kiểm.
- Mục 6.3: sửa cho khớp, nạp thêm `evidence.md`, và trỏ sang bảng ở mục 10 thay
  vì tự mô tả sai.
