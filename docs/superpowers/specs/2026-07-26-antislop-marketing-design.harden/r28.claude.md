ROUND: 28
AUTHOR: claude
READS: draft.md, r28.codex.md
VERDICT: needs-work
OPEN_BLOCKERS: 0
OPEN_MAJORS: 0

ACCEPT [major/testability] Tầng 3 không có tier và lang cho README, examples
  Đúng. scan bắt buộc hai tham số đó, mà tầng 3 chỉ nói "chạy scan lên README và
  examples". Không có cấu hình thì CI không chạy được, hoặc chạy với giá trị
  ngầm định do người viết CI tự chọn, và lúc đó tầng 3 kiểm một thứ khác với thứ
  spec mô tả.

ACCEPT [major/design] scan tính tier hiệu lực nhưng không xuất ra
  Đúng. Vòng 18 tôi cho scan.mjs tự nhận bảng số liệu và nhận tierMap, nhưng
  schema đầu ra không có chỗ nào thể hiện tier hiệu lực của từng khối. Nên
  antislop-check phải tự suy lại để biết khối nào áp chuẩn R. Hai bên suy độc
  lập là hai bên có thể lệch, và không ai biết bên nào đúng.

## Sửa trong draft.md

- Tầng 3: thêm `tests/scan-manifest.json` khai tier và lang cho từng file được
  soi. CI duyệt manifest và chạy từng dòng. Thêm file vào examples mà quên thêm
  dòng manifest thì CI báo thiếu chứ không im lặng bỏ qua; đó là cách duy nhất
  để tầng 3 không mục dần khi repo lớn lên.
- Giải thích lựa chọn tier cho README là P, vì nó là tài liệu giới thiệu và
  thuyết phục, gần proposal hơn báo cáo.
- Schema đầu ra scan.mjs thêm mảng `blocks` khai tier và lang hiệu lực của từng
  khối sau khi đã áp tierMap, bảng-tự-nhận và langMap, kèm `tier_source` nhận ba
  giá trị `document`, `data_table`, `tier_map`.
- Mỗi finding mang thêm `tier` là tier hiệu lực của khối chứa nó.
- Nguyên tắc: tính một lần, xuất ra, dùng chung.
