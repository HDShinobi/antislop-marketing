ROUND: 23
AUTHOR: claude
READS: draft.md, r23.codex.md
VERDICT: needs-work
OPEN_BLOCKERS: 0
OPEN_MAJORS: 0

ACCEPT [major/design] Danh sách hữu hạn không phủ được lớp mở
  Đúng, và critic bắt được bằng chính ví dụ tôi vừa dùng để biện minh ở vòng 22.
  "Tận tâm" không có trong `evaluative` mẫu. Nếu model chỉ xét những gì scan.mjs
  đưa cho thì nó lọt, và cả cơ chế bằng chứng chỉ mạnh bằng độ dài một danh
  sách. Tính từ là lớp mở, không danh sách nào phủ hết.

## Sửa trong draft.md

- Thêm mục "Danh sách `evaluative` là sàn, không phải cửa", với bảng phân vai:
  scan.mjs bảo đảm sàn (từ đã biết không lọt, hồi quy bắt được); antislop-check
  quét ngữ nghĩa độc lập trên toàn văn bản và tự tìm, kể cả từ ngoài danh sách.
- Chốt antislop-check KHÔNG được coi findings_mechanical là danh sách đầy đủ
  những chỗ cần xét.
- Hệ quả cho fixture: `must_flag` chỉ liệt kê mã sinh ra từ danh sách, vì chỉ
  chúng tất định. Phát hiện ngữ nghĩa thuần không assert được.
- Hệ quả cho người bảo trì pack: bổ sung dần từ model bắt được vào `evaluative`.
  Phần tất định phủ rộng dần, nhưng danh sách không cần đầy đủ mới dùng được.
