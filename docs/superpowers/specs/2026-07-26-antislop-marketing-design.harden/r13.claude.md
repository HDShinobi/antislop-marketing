ROUND: 13
AUTHOR: claude
READS: draft.md, r13.codex.md
VERDICT: needs-work
OPEN_BLOCKERS: 0
OPEN_MAJORS: 0

ACCEPT [major/testability] 6.5 liệt kê token trái với 6.2
  Đúng. Vòng 11 tôi chốt khối YAML là nơi duy nhất token tồn tại, nhưng để
  nguyên mục 6.5 vốn viết từ vòng 0 và đang liệt kê một bộ token khác. Skill đọc
  văn xuôi, scan.mjs đọc YAML, hai bộ rule khác nhau.

ACCEPT [major/testability] "Token dữ kiện" không có mẫu nhận diện
  Đúng. Tôi viết "chữ số, đơn vị, ngày tháng, URL, tên trường cấu hình" như một
  mô tả cho người đọc rồi đòi con số tất định. "Tên trường cấu hình" đặc biệt
  không có biên nào nếu để tự do.

ACCEPT [major/ambiguity] Sáu lớp từ mở đầu không phủ được token bất kỳ
  Đúng, và buộc phải sửa thiết kế chứ không chỉ thêm một dòng. Danh từ và động
  từ là lớp mở; tiếng Việt không có hình thái và không viết hoa danh từ chung
  nên phân biệt chúng ở token đầu tiên cần POS tagger. Không có lớp dự phòng thì
  same_shape_run không xác định cho câu thường.

## Sửa trong draft.md

- Mục 6.5 viết lại thành bảng ánh xạ nhóm sang trường trong pack, kèm ví dụ minh
  hoạ, và mở đầu bằng câu chốt "mục này mô tả, không liệt kê". Tách riêng hai
  quy tắc không thực thi bằng máy được (VI-NOMINALIZATION,
  VI-ADDRESS-CONSISTENCY) với mã riêng để check báo được còn scan.mjs bỏ qua.
- Mục 10: định nghĩa token dữ kiện bằng bốn mẫu chính xác. Ba mẫu đầu (số, ngày
  tháng, URL) trung tính ngôn ngữ và nằm trong core.md; mẫu thứ tư thành trường
  `config_tokens` trong pack. Bỏ hẳn "đơn vị đo" làm mẫu riêng vì đơn vị luôn đi
  kèm số nên mẫu số đã bắt, và một danh sách đơn vị đầy đủ không có điểm dừng.
- Mục 10: rút từ sáu lớp từ mở đầu xuống năm, bỏ danh_tu và dong_tu vì chúng là
  lớp mở. Bốn lớp còn lại đóng hoặc nhận diện bằng mẫu, cộng lớp `khac` dự
  phòng. Thêm cột thứ tự xét để một token trúng nhiều danh sách không nhập
  nhằng. Nói rõ phần lớn câu rơi vào `khac` và vì sao vẫn chấp nhận được.
- Schema pack: bỏ `openers.danh_tu` và `openers.dong_tu`, thêm `config_tokens`.
