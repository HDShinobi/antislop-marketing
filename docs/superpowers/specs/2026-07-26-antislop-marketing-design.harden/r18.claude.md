ROUND: 18
AUTHOR: claude
READS: draft.md, r18.codex.md
VERDICT: needs-work
OPEN_BLOCKERS: 0
OPEN_MAJORS: 0

ACCEPT [major/ambiguity] evaluative ở tier P: "khuyến khích" vs "cần chống lưng"
  Đúng, hai bảng nói ngược nhau. Mục 3.1 ghi "khuyến khích" từ vòng 0, mục 6.2
  ghi "cần chống lưng" từ vòng 14, và tôi không đối chiếu.

ACCEPT [major/testability] tier là một giá trị cho cả tài liệu, nhưng vùng số liệu phải là R
  Đúng. Quy tắc chống lỗi ghép ở mục 3.2 có từ vòng 0 và chưa bao giờ được nối
  vào giao diện scan.mjs. Hàm nhận đúng một tier thì không diễn đạt được
  "tài liệu này P nhưng khối 7 là R".

## Sửa trong draft.md

- Mục 3.1 và 6.2 đồng bộ về một chính sách: tier P bắt buộc chống lưng cho
  nhận định về thực tế, không bắt cho phát biểu về ý định.
- Thêm bốn ví dụ CẦN và KHÔNG CẦN, cùng bài phân biệt một câu: câu đó có thể sai
  không. Nhận định về thực tế thì có thể sai nên cần chống lưng; phát biểu về ý
  định thì không có đúng sai để kiểm.
- Chốt phép phân biệt này là ngữ nghĩa nên nằm ở findings_judged. scan.mjs vẫn
  báo mọi ứng viên eval_candidate ở tier P; lọc nhóm ý định là việc của model.
- Mục 10, giao diện: thêm `tierMap` theo chỉ số khối, song song với `langMap`.
  Chia vùng R làm ba đường theo mức độ máy nhận ra được: bảng Markdown thì
  scan.mjs tự nhận (cú pháp rõ, và gần như mọi bảng ở đây đều là bảng số liệu);
  khối khai trong tierMap thì người gọi chịu trách nhiệm; "mục kết quả" theo ngữ
  nghĩa thì chỉ model nhận ra nên vào findings_judged, không hứa ở tầng cơ học.
- Chốt thứ tự ưu tiên: tierMap thắng bảng-tự-nhận, bảng-tự-nhận thắng tier toàn
  tài liệu.
