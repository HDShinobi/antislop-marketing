ROUND: 7
AUTHOR: codex
READS: draft.md, r6.claude.md
VERDICT: needs-work
OPEN_BLOCKERS: 0
OPEN_MAJORS: 3

[SEVERITY: major]
LENS: design
LOCATION: Mục 10, “Văn bản song ngữ… gọi hai lần và cộng kết quả”
ISSUE: Cộng `same_shape_run` giữa các khối ngôn ngữ là sai vì đây là giá trị cực đại, nên hai khối đều có run 2 sẽ thành 4 và báo giả vượt ngưỡng 3.
SUGGESTION: Quy định cộng các bộ đếm, nhưng lấy `max()` cho `same_shape_run` (và giữ span theo từng khối).

[SEVERITY: major]
LENS: completeness
LOCATION: Mục 2, bảng “soát bản tiếng Việt” so với mục 5.4 và 10
ISSUE: Bộ nạp của `antislop-check` không gồm `evidence.md`, dù chính skill này phải xét `evidence_backing` và trả về rule `EVID-CANDIDATE`; đưa rule sang `core.md` sẽ vi phạm nguyên tắc rule chỉ tồn tại một chỗ.
SUGGESTION: Bổ sung `evidence.md` vào tập tài liệu nạp của check ở các tier có áp kiểm bằng chứng, và nêu rõ điều kiện nạp theo tier.

[SEVERITY: major]
LENS: ambiguity
LOCATION: Mục 6.3 và 10, hành vi với `lang` chưa đăng ký
ISSUE: Spec bảo `scan.mjs` vẫn chạy “nhịp câu” cho ngôn ngữ chưa đăng ký, nhưng `same_shape_run` lại bắt buộc bảng lớp từ mở đầu và token tack-on chỉ có trong language pack.
SUGGESTION: Chọn rõ một hành vi: bỏ `same_shape_run` cho ngôn ngữ chưa có pack, hoặc định nghĩa bảng phân lớp/tack-on trung tính trong `core.md`.