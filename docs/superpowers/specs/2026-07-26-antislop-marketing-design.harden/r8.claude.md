ROUND: 8
AUTHOR: claude
READS: draft.md, r8.codex.md
VERDICT: needs-work
OPEN_BLOCKERS: 0
OPEN_MAJORS: 0

ACCEPT [major/completeness] Tier P điều kiện (b) không kiểm được khi soát
  Đúng, và khác các vòng trước, đây là lỗ hổng mới chứ không phải hệ quả của bản
  sửa nào. `antislop-write` biết câu puffery từ đâu ra vì người dùng vừa đưa.
  `antislop-check` chạy trên file có sẵn thì mù hoàn toàn: nó không phân biệt
  được tagline khách đã duyệt với puffery model tự bịa. Điều kiện (b) tồn tại từ
  vòng 1 mà chưa bao giờ có đường kiểm.

ACCEPT [major/testability] Runner không nói plugin được cài vào backend kiểu gì
  Đúng, và hậu quả tệ hơn là chỉ thiếu bước. Một checkout mới không có skill
  trong registry, nên agent chạy mà không nạp `antislop-check`, và fixture cho
  kết quả vô nghĩa. Trường hợp xấu nhất là test xanh trong khi skill chưa từng
  được gọi.

## Sửa trong draft.md

- Mục 3.1, thêm "Nguồn gốc claim": file tuỳ chọn `.antislop-claims.txt` liệt kê
  claim đã duyệt, so khớp nguyên văn. Bảng ba tình huống. Chốt hành vi khi không
  có file là `EVID-PROVENANCE-UNKNOWN` xếp vào `judged` và **không tính vi
  phạm**, vì buộc tội một tagline khách đã duyệt đúng là kiểu dương tính giả mà
  `false-positives.md` sinh ra để chặn, và nó đắt hơn bỏ sót.
  Ghi rõ `antislop-write` không dùng file này; nó hỏi thẳng theo mục 4.1.
- Mục 10, phần Runner: thêm ba bước bắt buộc là cài, dò canary, gỡ. Bước canary
  là phần quan trọng nhất: chạy một prompt tối thiểu để xác nhận skill đã nạp
  thật, không thấy dòng khai báo tier thì dừng toàn bộ và báo lỗi môi trường chứ
  không chạy fixture.
- Mục 10: nói rõ lệnh cài cục bộ headless cho từng backend CHƯA chốt, và đưa nó
  vào danh sách phải kiểm chứng thay vì đoán. Đoán sai thì CI đỏ vì lý do không
  liên quan tới nội dung rule.
- Mục 12: thêm việc chặn v1 số 3, chốt lệnh cài và gỡ plugin cục bộ headless cho
  cả hai backend. Đánh số lại mục không chặn thành 4.
