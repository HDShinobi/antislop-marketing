ROUND: 1
AUTHOR: claude
READS: draft.md, r1.codex.md
VERDICT: needs-work
OPEN_BLOCKERS: 0
OPEN_MAJORS: 0

ACCEPT [major/scope] Thiếu dữ kiện khi viết
  Đúng, và đây là lỗ hổng nguy hiểm nhất trong sáu cái. Spec tuyên bố không sinh
  nội dung thay người dùng nhưng không quy định hành vi khi thiếu số, nên
  implementation sẽ bịa. Bịa số trong báo cáo hiệu quả quảng cáo là hỏng nặng
  nhất mà công cụ này có thể gây ra.

ACCEPT [major/completeness] "có điều kiện" ở tier P không định nghĩa
  Đúng. Tôi viết "có điều kiện" trong bảng 3.1 rồi không bao giờ định nghĩa
  điều kiện. Hai implementation sẽ xử lý khác nhau.

ACCEPT [major/testability] Tầng 2 không có test contract
  Đúng. Tôi mô tả một tầng test gọi model mà không nói assert vào đâu, trong khi
  đầu ra model không tất định.

ACCEPT [major/completeness] languages.json thiếu fallback và quy tắc đa ngôn ngữ
  Đúng. Mục 6.5 có nói về từ mượn tiếng Việt nhưng đó là quy tắc cục bộ của một
  pack, không phải hành vi hệ thống. Ngôn ngữ chưa đăng ký thì hoàn toàn không
  định nghĩa.

ACCEPT [major/assumptions] v1 cam kết "hiệu chỉnh" mâu thuẫn mục 7
  Đúng, tự mâu thuẫn thật. Mục 6.4 hứa v1 phát hành vi và en ở nhãn hiệu chỉnh,
  mục 7 lại cho phép phát hành với mục 7 ghi "chưa hiệu chỉnh".

ACCEPT [minor/ambiguity] Ghi đè không có bảng ánh xạ và thứ tự ưu tiên
  Đúng. "Gửi khách nhé" đổi tier hay đổi giọng là không rõ, và quan hệ giữa ghi
  đè với quy tắc R-cho-vùng-số-liệu chưa nói.

## Sửa trong draft.md

- Mục 3.1: thêm bảng "Điều kiện dùng puffery", định nghĩa riêng cho R, P, C.
  Tier C kiểm theo khối chứ không theo câu, vì ad copy tách chủ ngữ và dữ kiện
  ra hai câu liền nhau.
- Mục 3.2: thay đoạn ghi đè một dòng bằng bảng ánh xạ 5 dòng, thang ưu tiên 4
  cấp, và khẳng định chuẩn R cho vùng số liệu không ghi đè được.
- Mục 4.1 (mới): đầu vào tối thiểu theo tier, luật tuyệt đối không sinh dữ kiện,
  ba mức xử lý khi thiếu, nhãn `[cần ...]` để scan.mjs đếm được, và quy tắc cho
  số minh hoạ.
- Mục 6.3: thêm hành vi cho ngôn ngữ chưa đăng ký (nạp core.md một mình, khai
  báo rõ, không mượn pack gần giống) và phân biệt từ mượn với song ngữ thật.
- Mục 6.4: siết định nghĩa ba nhãn, điều kiện quyết định là mục 7. v1 phát hành
  cả vi và en ở nhãn cộng đồng. Nói rõ en cũng không phải hiệu chỉnh vì ngưỡng
  17-23 kế thừa không có nguồn.
- Mục 7: chốt việc đo không chặn v1, kèm lý do đánh đổi.
- Mục 10: thêm hệ mã rule ổn định, cờ `--json` với schema, ranh giới cái gì được
  assert (chỉ counted và tập rule; không assert span, text, judged), định dạng
  fixture must_flag / must_not_flag, và loại tầng 2 khỏi CI chặn merge.
- Mục 11: bổ sung hạng mục v1, ghi rõ nhãn cộng đồng.
- Mục 12: tách thành chặn v1 và không chặn v1; thêm việc chốt hệ mã rule.
