ROUND: 5
AUTHOR: claude
READS: draft.md, r5.codex.md
VERDICT: needs-work
OPEN_BLOCKERS: 0
OPEN_MAJORS: 0

ACCEPT [major/testability] Assert `counted` lấy từ đầu ra model thì flake
  Đúng, và nó phơi ra một lỗi tôi vừa tạo ở vòng 4. Vòng 4 tôi tách đúng
  `eval_candidate` khỏi `evidence_backing`, nhưng lại để nguyên chỗ nói fixture
  tầng 2 assert `counted` từ khối JSON của model. Model tự đếm thì không tất
  định, nên test vừa flake vừa không kiểm được `scan.mjs`, là thứ nó tưởng đang
  kiểm.

ACCEPT [major/ambiguity] Tier C vừa cần chống lưng vừa "không áp evidence"
  Đúng, mâu thuẫn trực tiếp giữa 3.1 và 5.4, và cũng do vòng 4 gây ra khi tôi
  siết tier C trong 3.1 mà quên dòng "Không áp ở tier C" ở 5.4.

## Sửa trong draft.md

- Mục 10 tầng 2: tách nguồn của từng phép assert. `counted` đối chiếu với đầu ra
  `scan.mjs` do runner tự chạy, không đối chiếu với JSON của model. Thêm bảng ba
  phép kiểm ghi rõ nguồn và cách khớp. Khối `counted` do model xuất chỉ ghi log
  để tham khảo; lệch nhiều là tín hiệu siết prompt, không phải test fail.
- Mục 5.4: thay dòng "Bắt buộc ở R, khuyến khích ở P, không áp ở C" bằng bảng
  hai cột tách đánh giá thông thường khỏi puffery và claim so sánh. Tier C không
  áp cho đánh giá thông thường nhưng bắt buộc áp cho puffery. Thêm hệ quả cho
  `antislop-check`: ở tier C chỉ chạy phép kiểm bằng chứng trên cụm được
  language pack đánh dấu là puffery hoặc hình thái so sánh, không chạy trên mọi
  tính từ.

## Ghi chú

Hai finding vòng này đều là lỗi nhất quán do bản sửa vòng 4 để lại, không phải
lỗ hổng thiết kế mới. Cả hai đều nằm gọn trong một mục và không kéo theo chỗ
khác. Khác về bản chất với các vòng trước, nơi mỗi bản sửa mở ra một câu hỏi
thiết kế chưa từng đặt.
