ROUND: 24
AUTHOR: claude
READS: draft.md, r24.codex.md
VERDICT: needs-work
OPEN_BLOCKERS: 0
OPEN_MAJORS: 0

ACCEPT [major/completeness] Lập luận lớp mở mới áp cho một trong ba danh sách
  Đúng. Vòng 23 tôi chốt `evaluative` là sàn chứ không phải cửa, rồi để nguyên
  `puffery` và `comparative` dù chúng cũng là lớp mở. Hệ quả tệ nhất nằm đúng ở
  tier C: tier này cho đánh giá thông thường đi tự do nhưng bắt puffery phải
  chống lưng, nên một claim mạnh ngoài danh sách sẽ bị xếp nhầm vào nhóm tự do
  và lọt hoàn toàn. Mà tier C chính là nơi ad copy sống.

## Sửa trong draft.md

- Tổng quát hoá "danh sách là sàn, không phải cửa" cho cả ba danh sách lớp mở:
  `evaluative`, `puffery`, `comparative`. Chỉ `banlist` và `mt_artifacts` là
  đóng thật, vì chúng liệt kê cụm cố định.
- Bảng phân vai đổi thành "scan.mjs + ba danh sách" bảo đảm sàn; antislop-check
  quét độc lập tìm mọi nhận định đánh giá, mọi claim marketing mạnh, mọi so sánh
  và cực cấp.
- Thêm bảng ba mã cho thứ model tự tìm được ngoài danh sách:
  `*-EVID-UNBACKED`, `*-PUFFERY-UNLISTED`, `*-COMPARATIVE-UNLISTED`.
- Nói rõ vì sao chỗ này quan trọng nhất ở tier C.
- Sửa câu hệ quả ở mục 5.4 cho khớp: "puffery" ở tier C không giới hạn trong
  danh sách của pack.
