ROUND: 12
AUTHOR: codex
READS: draft.md, r11.claude.md
VERDICT: needs-work
OPEN_BLOCKERS: 0
OPEN_MAJORS: 2

[SEVERITY: major]
LENS: completeness
LOCATION: mục 10, `eval_candidate`
ISSUE: `eval_candidate` cần danh sách “tính từ đánh giá” của language pack, nhưng schema `antislop-pack` chỉ có `banlist`; `scan.mjs` bị cấm parse văn xuôi nên không có dữ liệu để tính bộ đếm này.
SUGGESTION: Thêm trường machine-readable riêng (theo tier nếu cần) cho evaluative terms cùng rule ID, hoặc định nghĩa rõ `eval_candidate` chỉ dùng trường `banlist` và điều chỉnh phạm vi rule bằng chứng cho nhất quán.

[SEVERITY: major]
LENS: testability
LOCATION: mục 10, “Định nghĩa cùng khuôn”
ISSUE: Spec đòi fixture đếm chính xác `same_shape_run` nhưng không định nghĩa cách tách câu, nên dấu chấm trong số thập phân, URL, viết tắt, ellipsis hoặc Markdown sẽ cho kết quả khác nhau giữa các implementation.
SUGGESTION: Chốt thuật toán/phụ thuộc tách câu và phiên bản của nó, hoặc quy định đầy đủ các biên câu và trường hợp loại trừ trước khi dùng kết quả làm assert CI.