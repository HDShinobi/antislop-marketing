ROUND: 16
AUTHOR: codex
READS: draft.md, r15.claude.md
VERDICT: needs-work
OPEN_BLOCKERS: 0
OPEN_MAJORS: 3

[SEVERITY: major]
LENS: design
LOCATION: Mục 10, “Định nghĩa cùng khuôn”
ISSUE: Lớp dự phòng `khac` khiến ba câu trần thuật tự nhiên bất kỳ cùng số vế và không tack-on bị báo `same_shape_run >= 3`, dù không hề cùng khuôn.
SUGGESTION: Không cho hai câu cùng khuôn chỉ vì cùng rơi vào `khac`; yêu cầu lớp mở đầu đóng trùng nhau hoặc thêm tín hiệu phân biệt khác trước khi phát cảnh báo.

[SEVERITY: major]
LENS: testability
LOCATION: Mục 8.1 bảng “ĐẾM ĐƯỢC” so với mục 10
ISSUE: Bảng hiển thị “đánh giá không được chống lưng” là số đếm tái lập với ngưỡng 0, nhưng mục 10 quy định chống lưng là phán xét ngữ nghĩa không assert được; `eval_candidate` chỉ là ứng viên.
SUGGESTION: Đổi dòng bảng thành `eval_candidate` và chỉ báo số ứng viên, hoặc chuyển kết luận “không chống lưng” sang phần PHÁN ĐOÁN không có số/ngưỡng cơ học.

[SEVERITY: major]
LENS: completeness
LOCATION: Mục 10, “Giao diện của scan.mjs” và `findings_mechanical`
ISSUE: Spec buộc model chép nguyên từng `findings_mechanical` từ scan nhưng không định nghĩa schema/đầu ra finding của `scan.mjs`; API và fixture chỉ nêu `counted` cùng tập `rules`.
SUGGESTION: Chốt schema `scan.mjs` gồm từng finding cơ học (rule, span theo quy ước offset, text/lang nếu cần) và yêu cầu JSON skill sao chép mảng đó nguyên vẹn.