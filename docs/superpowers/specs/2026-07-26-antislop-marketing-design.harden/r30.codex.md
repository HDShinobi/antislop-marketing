ROUND: 30
AUTHOR: codex
READS: draft.md, r29.claude.md
VERDICT: needs-work
OPEN_BLOCKERS: 0
OPEN_MAJORS: 2

[SEVERITY: major]
LENS: ambiguity
LOCATION: Mục 3.2 “Chuẩn R cho vùng số liệu … không ghi đè được” vs. mục 10 “tierMap thắng bảng-tự-nhận”
ISSUE: `tierMap` có thể hạ một bảng số liệu tự nhận từ R xuống P/C, trái trực tiếp với quy tắc vùng số liệu luôn là R và không thể bị ghi đè.
SUGGESTION: Chốt `data_table` luôn thắng `tierMap`, hoặc chỉ cho `tierMap` nâng tier; cập nhật bảng ưu tiên và `tier_source` theo cùng quy tắc.

[SEVERITY: major]
LENS: testability
LOCATION: Mục 10, fixture tầng 2: `must_flag` và phép kiểm `findings_judged`
ISSUE: Fixture mẫu yêu cầu `VI-EVAL-CANDIDATE` và `VI-PUFFERY`, nhưng contract đặt các mã đó trong `findings_mechanical`, trong khi runner lại tìm `must_flag` chỉ trong `findings_judged`; test không thể đạt đúng contract.
SUGGESTION: Tách kỳ vọng thành `must_flag_mechanical` và `must_flag_judged`, hoặc đổi runner để kiểm mã cơ học ở `findings_mechanical` và chỉ kiểm mã phán xử ở `findings_judged`.

[SEVERITY: minor]
LENS: completeness
LOCATION: Mục 6.2 “khối JSON `antislop-pack`” và mục 10 “pack sai schema”
ISSUE: Spec không định nghĩa schema bắt buộc/tuỳ chọn của `antislop-pack`, nên không thể xác định nhất quán thế nào là pack “sai schema” hay kiểm CI cho pack mới.
SUGGESTION: Liệt kê các khoá bắt buộc, kiểu dữ liệu, giá trị hợp lệ và quy tắc cho khoá không biết; thêm fixture cho pack thiếu hoặc sai từng trường.