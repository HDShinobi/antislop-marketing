ROUND: 25
AUTHOR: claude
READS: draft.md, r25.codex.md
VERDICT: needs-work
OPEN_BLOCKERS: 0
OPEN_MAJORS: 0

ACCEPT [major/completeness] mt_artifacts nằm trong schema nhưng không có hành vi
  Đúng. Trường này có từ vòng 11 và chưa bao giờ được nối vào đâu: không mã
  rule, không bộ đếm, không hành vi theo tier, và bảng "bốn danh sách" bỏ qua
  nó. Nó là trường chết trong schema.

ACCEPT [minor/ambiguity] "Ký tự Unicode" không chỉ rõ đơn vị nào
  Đúng. Grapheme cluster, code point và UTF-16 code unit là ba thứ khác nhau, và
  JavaScript đánh chỉ số chuỗi theo cái thứ ba. Content marketing có emoji nên
  đây không phải trường hợp lý thuyết.

ACCEPT [minor/testability] Fixture tầng 2 khai bằng YAML nhưng không có parser
  Đúng, sót lại sau khi vòng 21 chuyển khối pack sang JSON. Cùng một lý do phải
  áp cho cùng một chỗ.

## Sửa trong draft.md

- `mt_artifacts` thành danh sách thứ năm có đủ hành vi: cấm ở cả ba tier, mã
  `*-MT-ARTIFACT`, có bộ đếm riêng. Bảng đổi tên thành "Năm danh sách, năm vai
  trò", thêm vào thứ tự ưu tiên so khớp, vào `counted` (chín khoá thay vì tám),
  vào bảng gộp khối, vào bảng ngôn ngữ chưa đăng ký, và vào bảng 8.1.
- `span` chốt là UTF-16 code unit, tức chỉ số chuỗi gốc của JavaScript. Lý do:
  đó là đơn vị `slice` dùng sẵn nên scan.mjs không phải chuyển đổi và `text`
  luôn khớp `span` bằng một phép cắt. Không dùng byte, không dùng grapheme
  cluster vì Node không có API dựng sẵn.
- Ghi rõ emoji ngoài mặt phẳng cơ bản chiếm hai code unit là hành vi đúng theo
  định nghĩa này. Bắt fixture cơ học có một mẫu emoji và một mẫu chữ có dấu, để
  quy ước bị kiểm chứ không chỉ được tuyên bố.
- Fixture tầng 2 chuyển sang JSON, đặt tên `<tên>.md` và `<tên>.expect.json`
  cùng quy ước với fixture cơ học.
