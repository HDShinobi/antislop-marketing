ROUND: 5
AUTHOR: codex
READS: draft.md, r4.claude.md
VERDICT: needs-work
OPEN_BLOCKERS: 0
OPEN_MAJORS: 2

[SEVERITY: major]
LENS: testability
LOCATION: Mục 10, Tầng 2: "`counted` bắt buộc khớp chính xác"
ISSUE: `antislop-check` là model-only nhưng spec lại assert chính xác các phép đếm và rule finding do model tự xuất, nên test sẽ flake và không thực sự kiểm `scan.mjs`.
SUGGESTION: Để runner tự chạy `scan.mjs` và assert kết quả đó; chỉ assert JSON của model về schema/contract và các finding ngữ nghĩa được chấp nhận theo cơ chế không-tất-định.

[SEVERITY: major]
LENS: ambiguity
LOCATION: Mục 3.1 so với mục 5.4: "Tier C ... dữ kiện chống lưng" / "`evidence.md` ... Không áp ở tier C"
ISSUE: Tier C vừa bắt buộc dữ kiện chống lưng cho puffery vừa nói tầng evidence không áp, nên không rõ checker có phải đánh giá bằng chứng cho puffery tier C hay không.
SUGGESTION: Nêu ngoại lệ rõ trong 5.4: evidence không áp cho đánh giá thông thường ở C, nhưng bắt buộc áp cho puffery/so sánh/cực cấp theo 3.1, kèm hành vi check tương ứng.