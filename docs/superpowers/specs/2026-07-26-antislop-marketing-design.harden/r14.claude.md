ROUND: 14
AUTHOR: claude
READS: draft.md, r14.codex.md
VERDICT: needs-work
OPEN_BLOCKERS: 0
OPEN_MAJORS: 0

ACCEPT [major/testability] Tier C không có trường nào để dò puffery và so sánh
  Đúng, và nó phơi ra chỗ schema đang làm việc chồng chéo. `banlist` chia theo
  tier vừa mang nghĩa "cấm tuyệt đối" vừa kiêm vai trò "puffery cần chống lưng",
  nên `banlist.C` rỗng là hợp lý theo cách đọc này và sai theo cách đọc kia. Tier
  C bắt buộc kiểm puffery, so sánh, cực cấp mà không có nguồn dữ liệu nào.

ACCEPT [minor/ambiguity] Điều kiện 1 và 2 của tách câu mâu thuẫn ở cuối khối
  Đúng. Điều kiện 1 cho phép hết khối, điều kiện 2 đòi phải có token tiếp theo.
  Dấu chấm cuối khối vừa thoả vừa không thoả.

## Sửa trong draft.md

- Schema pack: bỏ `banlist` chia theo tier, thay bằng bốn trường phẳng có vai trò
  tách bạch: `banlist` (cấm mọi tier), `puffery` (claim marketing mạnh),
  `comparative` (dấu hiệu so sánh và cực cấp), `evaluative` (đánh giá thông
  thường).
- Mục 6.2, thêm bảng "Bốn danh sách, bốn vai trò" ánh xạ từng trường sang hành vi
  ở R, P, C kèm mã rule. Tier quyết định danh sách nào có hiệu lực, danh sách
  không tự phân theo tier. Bảng này đọc thẳng thành code.
- `comparative` chỉ báo ứng viên; câu hỏi "mốc so sánh đã nêu chưa" là ngữ nghĩa
  nên vào `judged`, không vào `counted`. Cùng cách xử lý với `evidence_backing`.
- Mục 10, tách câu: đưa "hết khối luôn là ranh giới câu" lên thành quy tắc xét
  trước ba điều kiện, và bỏ vế "hoặc hết khối" khỏi điều kiện 1.
- Dọn ba chỗ còn tham chiếu `banlist.R/.P/.C` và `banlist.all`: bảng mục 6.5,
  bảng gộp song ngữ, bảng ngôn ngữ chưa đăng ký.
- Đổi tên một heading trùng do lint bắt.
