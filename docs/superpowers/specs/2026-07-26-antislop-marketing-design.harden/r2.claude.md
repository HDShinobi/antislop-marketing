ROUND: 2
AUTHOR: claude
READS: draft.md, r2.codex.md
VERDICT: needs-work
OPEN_BLOCKERS: 0
OPEN_MAJORS: 0

ACCEPT [major/scope] Tier R không có số thì không viết, quá chặt
  Đúng, và lỗi này do chính bản sửa vòng 1 của tôi đẻ ra. Audit tài khoản thường
  hoàn toàn định tính: tracking chưa cài, ba campaign broad match không có
  negative list, pixel không nối CAPI. Không có số nào nhưng đều kiểm chứng
  được, và audit nằm trong phạm vi v1 ngay từ mục 1. Quy tắc cũ chặn đúng loại
  tài liệu mà công cụ này sinh ra để phục vụ.

ACCEPT [major/testability] --json trên một SKILL.md thì không tồn tại
  Đúng. Tôi lẫn skill với chương trình. SKILL.md là prompt, không nhận cờ dòng
  lệnh, và không có entrypoint nào để fixture gọi. Contract vòng 1 không triển
  khai được.

## Sửa trong draft.md

- Mục 4.1: thêm bảng bốn dạng dữ kiện (số liệu, trạng thái cấu hình, mốc thời
  gian và sự kiện, tham chiếu). Đầu vào tối thiểu của tier R đổi từ "số liệu
  thật" thành "ít nhất một dữ kiện thuộc bốn dạng". Mức từ chối thứ 3 đổi theo.
- Mục 5.4: sửa lan theo. Quy tắc bằng chứng đổi từ "phải kèm một con số" thành
  "phải kèm một dữ kiện kiểm chứng được", trỏ về bốn dạng ở mục 4.1. Thêm hai ví
  dụ định tính (tracking, cấu trúc tài khoản) để chỗ này không bị đọc thành
  quy tắc chỉ-số-mới-tính.
- Mục 10: bỏ khung `--json`. Contract nói bằng ngôn ngữ của skill: yêu cầu chứa
  từ `json` thì skill in thêm khối rào ```json ở cuối phản hồi.
- Mục 10: thêm runner `tests/fixtures.mjs` gọi agent headless qua `claude -p`
  hoặc `codex exec`, chọn bằng `ANTISLOP_RUNNER`, bắt stdout, lấy khối JSON
  cuối cùng. JSON thiếu hoặc không parse được thì tính fail, vì đó là tín hiệu
  contract đầu ra bị trôi.
- Mục 11: sửa cách gọi tên hạng mục cho khớp mục 10.
