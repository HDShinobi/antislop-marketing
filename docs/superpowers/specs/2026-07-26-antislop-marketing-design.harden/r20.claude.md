ROUND: 20
AUTHOR: claude
READS: draft.md, r20.codex.md
VERDICT: needs-work
OPEN_BLOCKERS: 0
OPEN_MAJORS: 0

ACCEPT [major/testability] Không nói antislop-check tìm scan.mjs bằng cách nào
  Đúng, và nó làm hỏng thứ vừa dựng ở vòng 10. `node tests/scan.mjs` chỉ chạy
  được khi CWD tình cờ là checkout của repo. Người dùng thật chạy skill ở thư
  mục của họ, nên counted_source sẽ luôn rơi về "model" và toàn bộ cơ chế
  tất định thành vô dụng ngoài CI.

ACCEPT [minor/ambiguity] Phân bố độ dài câu không có chỗ trong schema tám khoá
  Đúng. Bảng ngôn ngữ chưa đăng ký bảo in phân bố, nhưng schema chốt đúng tám
  khoá và không có trường nào chứa nó. Mà cũng không có ngưỡng nào để so, nên in
  ra không dùng vào việc gì.

## Sửa trong draft.md

- Chuyển `scan.mjs` từ `tests/` sang `bin/`, vì nó là thành phần chạy lúc dùng
  chứ không phải công cụ kiểm thử. Cập nhật cây thư mục ở mục 2, dạng CLI, và
  mọi tham chiếu. `tests/scan.test.mjs` import từ `../bin/scan.mjs`.
- Thêm mục "Tìm scan.mjs từ một thư mục làm việc bất kỳ": skill giải đường dẫn
  theo thư mục của chính nó do harness cung cấp, không theo CWD. Từ
  `skills/antislop-check/` thì là `../../bin/scan.mjs`.
- Gộp việc này với câu hỏi đường dẫn `references/` ở mục 12 thành một hạng mục
  kiểm chứng, vì đó cùng một câu hỏi. Nếu một harness không resolve được thì cả
  hai cùng rơi về phương án dự phòng và counted_source về "model" ở harness đó.
- Bắt fixture có ít nhất một lần chạy với CWD ngoài repo, nếu không lỗi này
  không bao giờ lộ trong CI.
- Bỏ yêu cầu in phân bố độ dài câu cho ngôn ngữ chưa đăng ký. Không có ngưỡng để
  so thì in ra không dùng vào việc gì, và giữ schema đúng tám khoá.
- Sửa câu mô tả tầng 1 cho khớp tám khoá thay vì liệt kê cũ.
