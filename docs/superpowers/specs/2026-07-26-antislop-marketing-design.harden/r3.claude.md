ROUND: 3
AUTHOR: claude
READS: draft.md, r3.codex.md
VERDICT: needs-work
OPEN_BLOCKERS: 0
OPEN_MAJORS: 0

ACCEPT [major/design] "kèm dữ kiện" không đòi dữ kiện chứng minh claim
  Đúng, và đây là lỗi logic thật chứ không phải lỗi diễn đạt. "Đội ngũ tận tâm,
  CPA 31$" thoả quy tắc cũ theo đúng nghĩa đen mà không chứng minh gì. Quy tắc
  cũ kiểm sự có mặt của dữ kiện, không kiểm quan hệ chứng minh. Đây chính là
  cách một model sẽ lách khi bị ép phải có số.

ACCEPT [major/testability] Tầng 1 không có fixture, runner bỏ qua counted
  Đúng, hai lỗ hổng riêng biệt trong cùng một chỗ. Tầng 1 là hàm thuần nhưng
  không có gì kiểm nó. Runner tầng 2 thì tôi tuyên bố assert cả counted lẫn tập
  rule nhưng phần mô tả cụ thể chỉ nói tới hai tập rule.

ACCEPT [major/ambiguity] 5.4 đặt bảng tier vào vi.md, trái 6.1 và 6.2
  Đúng. Ba dòng cuối mục 5.4 đứng ngay sau dòng nói về vi.md nên đọc ra thành
  nội dung của vi.md, trong khi 6.1 nói core.md chứa định nghĩa tier và mức, còn
  6.2 chỉ cho phép đúng tám mục không gồm chúng.

## Sửa trong draft.md

- Mục 5.4: đổi "kèm" thành "chống lưng", định nghĩa chống lưng là dữ kiện chứng
  minh chính tính từ đó. Thêm cặp ví dụ KHÔNG ĐẠT / ĐẠT dùng đúng ca "đội ngũ
  tận tâm, CPA 31$" mà critic nêu. Thêm bài kiểm một câu: xoá tính từ đi, dữ
  kiện còn lại có tự nói lên điều đó không. Thêm hai đường ra khi không chống
  lưng được.
- Mục 5.4: viết lại ba dòng cuối thành danh sách rõ ràng, khẳng định bảng quyết
  định tier, ba mức và dòng khai báo nằm duy nhất trong core.md; language pack
  chỉ đóng góp mục 8 là danh sách từ khoá.
- Mục 10 tầng 1: thêm `tests/fixtures/mechanical/` với cặp `.md` và
  `.expect.json`, cùng ba điều kiện fail làm CI đỏ.
- Mục 10 tầng 2: fixture khai thêm khối `counted` khớp chính xác; hai tập rule
  khớp theo quan hệ tập hợp. Thay đoạn văn ranh giới assert bằng bảng năm dòng
  ghi rõ trường nào assert và vì sao.
- Đổi tên nhất quán `EVID-NO-NUMBER` thành `EVID-UNBACKED` và
  `eval_no_number` thành `eval_unbacked` trong schema JSON và bảng mục 8.1, vì
  quy tắc không còn dựa trên sự có mặt của con số.
