ROUND: 25
AUTHOR: codex
READS: draft.md, r24.claude.md
VERDICT: needs-work
OPEN_BLOCKERS: 0
OPEN_MAJORS: 1

[SEVERITY: major]
LENS: completeness
LOCATION: Mục 6.2 `mt_artifacts` và mục 10 “Tầng 1”
ISSUE: `mt_artifacts` được đưa vào schema pack như một rule thực thi nhưng không có hành vi theo tier, mã rule, bộ đếm, finding cơ học, hay phán xử nào; scanner chỉ quét bốn danh sách khác.
SUGGESTION: Chốt `mt_artifacts` phải bị báo như thế nào (mã, tier, tầng scan/model và schema), hoặc bỏ trường này và tuyên bố rõ nó ngoài phạm vi v1.

[SEVERITY: minor]
LENS: ambiguity
LOCATION: Mục 10, quy ước `span`
ISSUE: “ký tự Unicode” không xác định là grapheme cluster, code point hay UTF-16 code unit, trong khi JavaScript index chuỗi theo UTF-16 và content marketing có thể chứa emoji.
SUGGESTION: Chọn một đơn vị offset có thể triển khai trong Node (ví dụ UTF-16 code unit) hoặc quy định chuyển đổi sang Unicode code point, rồi thêm fixture chứa emoji và ký tự có dấu.

[SEVERITY: minor]
LENS: testability
LOCATION: Mục 10, fixture tầng 2
ISSUE: Fixture kỳ vọng được mô tả dưới dạng YAML nhưng runner `fixtures.mjs` không được phép/phụ thuộc parser YAML nào và format tệp không được chốt.
SUGGESTION: Chốt fixture tầng 2 là JSON hoặc khai báo parser YAML được dùng, định dạng tên tệp, và schema validation tương ứng.